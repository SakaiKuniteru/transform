<div class="form-actions">
    {{#each actions}}
        <button type="{{type}}" class="form-action{{#if className}} {{className}}{{/if}}"{{#if name}} name="{{name}}"{{/if}}{{#if value}} value="{{value}}"{{/if}}{{#if disabled}} disabled{{/if}}>{{label}}</button>
    {{/each}}
</div>